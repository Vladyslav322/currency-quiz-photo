import React from 'react';
import classes from './PhotoGramm.module.scss';

const PhotoGramm = () => {
    return (
        <div className={classes.photoGramm__container}>
            photo
        </div>
    );
};

export default PhotoGramm;
